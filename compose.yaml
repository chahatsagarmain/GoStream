version: "3.8"

services:
  go-stream:
    build:
      context: ./       
      dockerfile: Dockerfile
    container_name: go-stream
    depends_on:
      redis:
        condition: service_healthy
    ports:
      - "8080:8080"
      - "9090:9090"